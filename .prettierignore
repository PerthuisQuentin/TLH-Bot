# Prettier owns source code here, nothing else.

dist
node_modules

# npm owns the formatting of these two; letting Prettier reindent them just
# churns the diff on the next install.
package.json
package-lock.json

# Per-guild game data. `files/` is written by the bot at runtime, and the two
# JSON files at the root are real data checked in, not configuration.
files
shells.json
config.json
